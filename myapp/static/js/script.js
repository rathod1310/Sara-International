/* =========================================
   Sara International – script.js
   ========================================= */

document.addEventListener('DOMContentLoaded', function () {
  // All initialization is handled by main.js
  // initMegaMenu() in main.js now handles both desktop hover
  // and mobile click toggle — no duplicate calls needed here
});